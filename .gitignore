# Build artifacts
*.zip
!treaducation/treaducation.zip

# Local deployment config (contains server IPs, paths, credentials)
.deploy.conf

# Audio working files
src/audio/*.bak
src/audio/preview/

# Local-only directories (kept locally, not tracked in git)
analysis/
data/
