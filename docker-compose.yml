services:
  treads:
    image: nginx:alpine
    ports:
      - "53312:80"
    volumes:
      - ./src:/usr/share/nginx/html:ro
    restart: unless-stopped # set to 'always' if you want it to auto-recover
